<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appiscina">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.telaEditarPerfil"></activity>
        <activity android:name=".views.telaPerfil" />
        <activity android:name=".views.tela2CadastroPropriedade" />
        <activity android:name=".views.telaCadastroPropriedade" />
        <activity android:name=".views.addPropriedade" />
        <activity android:name=".views.telaRedefinicaoSenha" />
        <activity android:name=".views.MainActivity" />
        <activity android:name=".views.telaCadastroUsuario" />
        <activity android:name=".views.telaSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.telaLogin">
            <intent-filter>
                <action android:name="com.example.appiscina.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>